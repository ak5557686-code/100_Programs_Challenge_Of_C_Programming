//Purpose: The following program calculates and prints the area of a square on screen with given height and width.
#include <stdio.h>
#include <conio.h>
int main(){
    int height,width,Area;
    height=5;
    width=4;
    Area=height*width;
    printf("Area of the square is :%d",Area);
    getch();
}